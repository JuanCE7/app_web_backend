[phases.setup]
# Unificado con el Dockerfile (Node 20) para evitar divergencias entre builders.
nixPkgs = ["nodejs_20"]

[phases.build]
cmds = ["npm install", "npm run build"]

[phases.start]
cmd = "npm run start:prod"
